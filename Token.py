# Sonia
# 11 08 21


# Token is a component resultant from breaking the input.
# Token can be defined as an object with several properties.


class Token:

    def __init__(self, type, value, index):

        self.type = type
        self.value = value
        self.index = index
